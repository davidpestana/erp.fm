{% extends 'erpBundle::layout.modal.html.twig' %}

{% block title -%} Editar direccion {% endblock %}

{% block body -%}
    <form action="{{ path('direcciones_update', { 'id':entity.id }) }}" method="POST" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
{#         <button type="submit" class="btn blue">Guardar cambios</button>#}
    </form>
    
    <form action="{{ path('direcciones_delete', { 'id': entity.id }) }}" method="POST">
        <input type="hidden" name="_method" value="DELETE" />
        {{ form(delete_form) }}
{#         <button type="submit" class="btn red">Eliminar direccion</button>#}
    </form>
{% endblock %}
