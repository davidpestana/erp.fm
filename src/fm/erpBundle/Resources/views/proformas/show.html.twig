{% extends "erpBundle::layout.modal.html.twig" %}

{% block title -%} Factura Proforma {{ entity.codproforma }}{% endblock %}

{% block body -%}

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Codproforma</th>
                <td>{{ entity.codproforma }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ entity.fecha|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ entity.observaciones }}</td>
            </tr>
            <tr>
                <th>Cliente</th>
                <td>{{ entity.cliente }}</td>
            </tr>
        </tbody>
    </table>
        {% for producto in entity.productos  %}
            {{ producto.precio }}
        {% endfor %}

    AQUI EL PDF EN UN IFRAME

{% endblock %}
