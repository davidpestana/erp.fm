{% extends '::base.html.twig' %}

{% block body -%}
    <h1>cajones</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Alto</th>
                <td>{{ entity.alto }}</td>
            </tr>
            <tr>
                <th>Ancho</th>
                <td>{{ entity.ancho }}</td>
            </tr>
            <tr>
                <th>Largo</th>
                <td>{{ entity.largo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cajones') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cajones_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
