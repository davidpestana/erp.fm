{% extends "erpBundle::layout.html.twig" %}

{% block title -%} Cajones para los Kits{% endblock %}

{% block body -%}

    <table class="table table-striped table-bordered table-hover table-full-width" id="sample_2">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Alto</th>
                <th>Ancho</th>
                <th>Largo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cajones_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.alto }}</td>
                <td>{{ entity.ancho }}</td>
                <td>{{ entity.largo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cajones_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cajones_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cajones_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
