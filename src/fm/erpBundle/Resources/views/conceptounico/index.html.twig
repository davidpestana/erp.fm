{% extends 'erpBundle::layout.html.twig' %}

{% block title -%} Lista de Conceptos únicos {% endblock %}
{% block body -%}
    <table class="table table-striped table-bordered table-hover table-full-width" id="sample_2">
        <thead>
            <tr>
                <th>Id</th>
                <th>Concepto</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('conceptounico_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.concepto }}</td>
                <td>
                    {#<a href="{{ path('conceptounico_show', { 'id': entity.id }) }}"class="modal_ajax_btn btn blue mini" data-toggle="modal"> Ver</a>#}
                    <a href="{{ path('conceptounico_edit', { 'id': entity.id }) }}" class="modal_ajax_btn btn red mini" data-toggle="modal"> Editar</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('conceptounico_new') }}" class="modal_ajax_btn btn red" data-toggle="modal"> Crear nuevo Concepto</a>
{% endblock %}
